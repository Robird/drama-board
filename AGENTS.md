# DramaBoard 仓库协作约定

## 项目入口与依据

- DramaBoard 是早期实验项目，玩法、架构与 API 仍可随证据修订。
- 非简单任务先完整阅读根目录 [PROJECT-STATE.md](PROJECT-STATE.md)，再按当前问题读取相关源码、测试和设计章节；不要求顺读历史文档。
- `PROJECT-STATE.md` 是跨会话导航与工作记忆。区分用户已定方向、源码实现事实、建议和待决问题；它不替代当前用户要求或适用的协作约定。
- 设计文档描述意图，当前源码、测试和实际运行结果证明实现。历史文档中的任务提示、授权和“下一轮建议”保留其历史语境，不自动成为本轮任务。
- 文档分类见 [docs/README.md](docs/README.md)。历史正文 `docs/archive/legacy/` 已从默认 rg 检索排除；仅在问题需要时按[历史索引](docs/archive/README.md)读取，或用 `rg --no-ignore -n "关键词" docs/archive/legacy` 显式查找。

## 自主维护项目记忆

- 当前焦点、后续依赖顺序、重要裁决或能力边界发生实质变化时，主动更新 `PROJECT-STATE.md`；无需等待用户另行要求。
- 保留最终目标、长期 roadmap、眼下的问题、下一步及其验收点、延期事项的重开条件。建议被采纳或否决后替换原条目，不积累讨论过程。
- 完成一项工作后，删除对应施工待办并改写当前焦点；完成事实最多保留一句结论和源码、类型或证据链接。
- 不追加逐轮日志、已完成清单、测试数量、命令输出或对话摘录。详细证据留在测试、相关设计/实验文档或 Git；不写凭据和私有推理。
- 以一屏到数屏可读完为目标，通常控制在约 100 行内；增长时先合并、替换和删除过时内容。确需展开的专题另存并链接，主文件仍保留方向与重开条件。
- 同一种当前信息只维护一处。README 提供入口，AGENTS 保存稳定协作规则，PROJECT-STATE 保存动态项目状态；不要另建同义的 checkpoint 或维护历史状态表。

## Subagent 分工

- 可用简短任务说明独立完成的事实性只读调查，优先委派给 `gpt-5.6-terra` 或 `gpt-5.6-luna`，使用 `fork_turns: "none"`。几次搜索即可完成的小问题留在主线程，不为分工而分工。
- 调查任务须自包含：给出具体问题、仓库与文件入口、已定约束、只读范围及预期输出；要求返回文件/类型位置、支持结论的证据和仍不确定的问题，避免搬运大段原文。
- 设计裁决、正确性证明、关键不变量审查，以及依赖大量对话历史的工作，默认继承主代理模型与上下文：省略 `model`、`reasoning_effort`，使用 `fork_turns: "all"`。不能仅因任务只读就降到小模型；调查发现此类问题时交回主代理。
- 显式指定模型时不与 `fork_turns: "all"` 混用；确需最近少量对话时可用正整数字符串。主代理核对调查证据；若反复补上下文或重做调查，改回默认方式。

## C# 代码风格

- 采用 [.editorconfig](.editorconfig) 中的 K&R 风格：左大括号与声明/控制语句同一行，`else`、`catch`、`finally` 跟随前一个右大括号。
- 新增和实质修改的代码采用该风格；旧代码随相关修改逐步调整，不为了统一格式批量重排无关文件或手改生成代码。

## 修改与验证

- 在当前任务范围内推进；维护项目记忆时不要把尚未裁决的候选方案写成已批准设计。
- 纯文档修改检查集成 diff、受影响链接和锚点；代码、项目或依赖修改按影响范围构建并验证相关行为。当前 solution 与依赖入口见 PROJECT-STATE。
- 验证结论说明证据范围；读过测试代码不等于本轮运行通过。已过时的历史结果不作为当前基线。
