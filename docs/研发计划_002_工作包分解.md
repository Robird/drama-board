# 研发计划 002：工作包分解

**状态：活跃维护中**
**日期：2026-08-16**
**定位：DramaBoard 第一阶段（DEVS-like Simulation Kernel 探索）的工作包清单与状态跟踪。本文档是主线会话与 coder subagent 之间的共享状态，每个 WP 完成后主线更新状态表。**

前提文档：
- `docs/研发计划_001_架构基线与决策记录.md`（必读，D1–D8）
- `docs/开放世界棋盘游戏设计_003_Forecast_Elapse_Decide_SimulationKernel.md`（Kernel 语义之源）
- `docs/基础设施.md`（优先级依据）

---

## 自主循环协议（2026-08-16 起生效）

用户以极简循环提示词驱动开发，主线会话自主规划与推进。用户标准提示词：

> **继续 DramaBoard 自主开发。按 docs/研发计划_002 的状态表、"下一轮建议"与 repo memory 恢复上下文，自主规划本轮目标并推进；结束时更新状态表与"下一轮建议"并汇报。遇到方向级决策点列出选项与你的倾向，可先按倾向推进不阻塞。**

主线每轮流程（OODA 映射）：
1. **Observe**：读 `agent-checkpoint/memory-notebook.md`（主线跨压缩快照）+ 状态表 + 下一轮建议 + memory + `git log`；跑 `dotnet test` 确认基线。
2. **Orient**：对照 Design Note 001–003 与研发计划_001，评估价值最高的推进方向。
3. **Decide**：定本轮目标（工作包 / 架构决策 / 重构），必要时先写决策再动工。
4. **Act**：细化派发 coder → 独立验收（跑测试 + 审关键接口）→ 逐包 commit → 更新状态表。
5. **Report**：汇报产出、涌现的设计发现、更新"下一轮建议"与 checkpoint（上下文将满时），方向级问题显式列出供用户异步纠偏。

授权边界：
- **自主**：WP 推进与细化、接口探索、Kernel 内决策（记入文档）、测试策略、本地 commit、文档更新、用不同模型 subagent 做异构评审。
- **停下来问**：游戏设计层面的取舍（001 愿景级）、大规模返工（>1 个 WP 的推翻）、引入重量级依赖、修改 atelia repo、任何 push / 历史改写。
- 防漂移机制：设计文档 001–003 是方向宪法；每轮汇报必含"下轮打算"，用户可异步否决；方向级决策在状态表留档可回溯。

### 下一轮建议（每轮结束更新）

1. **WP10 FirstBoard 派发**：前置包已全部完成。主线需先细化规格：①同批多决策提交策略已倾向逐个提交（每决策立即作为输入跑 Run 再问下一个，同刻公平性由 A4 仲裁模式承担），需在 PlayerDecisionSession 落实；②世界模型：3 Places、2 Actors、1 Object、1 Secret、1 Deadline，六动作 Travel/Wait/Talk/Observe/Take/Give；③Intent→领域输入的验证翻译与 Observation 投影（信息不对称：Observation 只含角色在场可见信息）；④建议新建 tests/FirstBoard.Tests 或在 Host.Tests 下建 FirstBoard 子目录，完整跑一局并输出 journal。
2. **WP11 前置**：A5 provenance（事件带 Cause+resolve 批次）与 Version 下沉 codec——envelope 变更必须在落盘前完成。可在 WP10 后、WP11 前一并做。
3. 可选：对 Host/Protocol 层再做一次小规模 adversarial review（重点：同批决策、取消恢复、迟到决策），在 FirstBoard 稳定后收益更大。

---

## 状态表

| WP | 名称 | 状态 | 备注 |
|----|------|------|------|
| WP0 | Solution 骨架 | 完成 | 2026-08-16 由 coder 完成，主线验收通过。slnx；xUnit v2 2.9.3（v3 模板不可用）；测试 1/1 绿 |
| WP1 | 时间语义 primitives | 完成 | 2026-08-16 完成；WP4 将 tick 从 1 秒调整为 1 毫秒，增加 Zero/FromSeconds/FromMilliseconds；溢出 checked |
| WP2 | 确定性事件队列 | 完成 | 2026-08-16 完成。Due=ModelTime（Microstep 归 Resolve 阶段）；泛型 EventCandidate；tie-break=(Due,SourceId,CandidateId)；失效带记忆（stale 迟到入队直接进 invalidated）。30 测试绿 |
| WP3 | Kernel 主循环 MVP + Timer 玩具模型 | 完成 | 2026-08-16 完成；35 测试绿。ISimSystem<TWorld,TCand,TEvt> 三泛型；Resolve 返回 ResolveResult(新 world+事件)；Microstep 由 Loop 按提交序分配、跨时刻重置。遗留张力：①三泛型迫使异构系统共享 payload 联合类型（WP4 弹球+改道双系统压测此点）②全量重 Forecast 下 ForecastQueue 的 InvalidateSource 未被 Loop 使用，失效表现为"下轮不再产生"——WP4 需验证该语义足够，否则引入跨轮持久队列 |
| WP4 | Forecast 失效玩具模型（改道 + 弹球） | 完成 | 2026-08-16 完成；43 测试绿。tick 粒度改 1ms；全量重 Forecast 足以表达改道失效；弹球采用毫秒向上量化并在精确碰撞点换速；payload 联合类型摩擦中等（系统增多前需 typed adapter 或事件 envelope）。新张力：①Loop owner map 按 CandidateId 索引，隐含跨 source 全局唯一假设→WP5 修；②毫秒量化下近同时碰撞被顺序 Resolve 掩盖→同刻闭包/批量候选语义留给 WP6 前定义 |
| WP5 | Deterministic RNG + 随机 Forecast | 完成 | 2026-08-16 完成；56 测试绿。纯函数坐标寻址 RNG（SplitMix64 混合，(worldSeed,streamId,generation,sampleIndex)→样本）；指数采样自实现 ln 级数不依赖 Math.Log，位模式锁定；owner map 修为 (SourceId,CandidateId)。裁决 WP4 张力②：第一阶段不引入 μ 阶段体系，锁定"顺序 Resolve+确定 tie-break+事件后重 Forecast"为正式同刻语义（WP6 加锁定测试）。WP7 注意：RNG 常量与派生规则属 replay 兼容契约 |
| WP6 | Activity 与中断玩具模型 | 完成 | 2026-08-16 完成；62 测试绿。Activity 用 StartedAt 惰性状态、中断时 materialize 进度入事件 payload；同刻语义（顺序 Resolve+因果穿透）已由测试锁定为契约；Kernel 零改动扛住全部玩具模型 |
| WP7 | Replay/Fork harness + property tests | 完成 | 2026-08-16 完成；76 测试绿。双 replay：re-run determinism（五模型全覆盖）+ reducer fold（Timer/InterruptedMining 探索）；Fork 前缀折叠重建+分支验证（含随机发现前缀共享）；CsCheck 4.8.0（仅测试项目）锁 6 条不变量。设计结论：reducer 应提升为独立正式契约（IEventReducer），但待事件 envelope 定型后——见下方"第一阶段结论" |
| WP8 | DramaBoard.Protocol | 完成 | 2026-08-16 完成；99 测试绿（后续修复包后 108）。DTO：DecisionRequest/PlayerDecision/Intent/ExpectedOutcome/Observation/KnownFact/AvailableAction；包装值类型 DecisionId/ActionKind/DecisionReason/FactKind（wire 形状为 string）。裁决：零依赖（不引用 Kernel，Host 是装配点）；时间/版本用原始 long（DTO 是 wire 形状）；Intent 第一版扁平不做多态 JSON，六动作表达力已测试锁定。张力：BasedOnWorldVersion 在 Kernel 无生产者——评审 A3 已立案，WP9 解决 |
| WP9 | IPlayerDriver + DecisionPoint 集成 | 完成 | 2026-08-17 完成（拆 WP9a/WP9b）；127 测试绿。**WP9a Kernel 侧**：SimulationCursor（跨 Run 循环控制状态，不可变 record）、StopReason（Exhausted/BoundaryReached/DecisionRequired）、决策停机谓词（构造注入 Func<DomainEvent,bool>，控制流不入 journal）、显式输入端口（externalInputs 在 cursor.Now 提交、过 reducer 入 journal——**replay 只需 initialWorld+journal，修评审 A1**）、WorldVersion=(LineageId,EventCount)、SourceId 从世界持久实体身份派生（修 A4 身份部分）、删除未被使用的 Generation/InvalidateSource 假承诺机制。切分等价 CsCheck property 锁定（注：严格陈述为"inputs 在相同模型时刻注入时切分等价"——输入落在 cursor.Now 是契约）。活锁预算迁入 cursor 跨 Run 不重置（修 A6b）。**WP9b Host 侧**：DramaBoard.Host 项目（装配点，引用 Kernel+Protocol，零领域内容）：IPlayerDriver + Null/Scripted/Random(SplitMix64) 三实现；PlayerDecisionSession 编排器（单线程顺序，领域翻译由 requestBuilder/decisionTranslator 注入，版本不匹配即抛）；async 边界仅在 Host。Protocol 补 LineageId/Microstep 字段（修 A3）。端到端：决策改变世界线+journal 纯 fold 重建不需 driver。张力留档：同批多决策不能观察彼此结果（WP10 需裁决批处理/逐个提交）；async 取消在"请求已入 journal 输入未提交"时的恢复留待 speculative 阶段 | |
| WP10 | FirstBoard 场景 | 未开始 | 依赖 WP9。**前置包已完成（2026-08-17，136 测试绿）**：A9 动态实体生死（Kernel 零改动，生灭世界模型锁定：杀死/spawn/外部输入 spawn）；A4 顺序无关仲裁模式（夹宝模型：先 Resolve 者只是执行仲裁计算的人，胜者由坐标寻址 RNG 决定，交换 Resolve 顺序胜者不变；tie-break 从此只决定执行顺序不决定游戏结果）；A8 RNG 坐标纪律（DeriveStreamId(long/string) 入 Kernel，采样坐标入事件 payload，journal 自含可审计——fold 时重算 RNG 可验证）；A6d property 覆盖全部 9 个玩具模型 system，**抓到并修复 Bouncing/InterruptedMining 的真实 now 依赖**。FirstBoard 本体待派发 |
| WP11 | EventJournal 落盘 adapter | 未开始 | 依赖 WP7，方向性 |

状态取值：未开始 / 进行中 / 待验收 / 完成 / 返工中。

### 第一阶段结论（2026-08-16，WP0–WP7 完成后）

Kernel 原型目标达成：确定性循环、同刻语义、坐标寻址 RNG、Activity/中断、replay/fork 均被测试锁定，Kernel 零依赖、零改动扛住五类玩具模型。

进入 WP8/WP9 前需先做的架构决策（候选 D9）：
1. **统一事件 envelope**：当前 DomainEvent.Kind 是 string、payload 是各模型自定义联合类型；多系统共存需要统一信封（含未来序列化/versioning 考虑，与 WP8 Protocol、WP11 落盘直接相关）。
2. **IEventReducer 正式契约**：独立于 ISimSystem，使状态投影在机制上由已提交事件产生，封闭"ResolveResult.World 可与事件描述不一致"的缺口（D2 的最后一公里）。
3. RNG 算法常量与派生规则自 WP7 起视为 replay 兼容契约，变更需显式版本化。

**D9 已裁决并落实（2026-08-16，同日）**：强制 event-sourcing（方案 a）+ EventKind(string Id, ushort Version) envelope + 空事件死循环防护 + 全局 reducer 路由。旧路径（ResolveResult 双轨）删除，五模型+全部 harness 迁移，Bouncing payload 经每球局部时间锚点瘦身（五球二球碰撞 40→13 标量）。87 测试绿。正式条目见研发计划_001 D9。落实过程 spike 实证：方案 b 的 property test 只能覆盖已测场景，方案 a 从类型上消除第二条状态写入通道。

分层依赖：

```text
WP0 骨架
 └─ WP1 时间 ── WP2 队列 ── WP3 循环+Timer
                              ├─ WP4 失效模型 ── WP6 Activity中断
                              ├─ WP5 RNG ────┐
                              │              ├─ WP7 Replay/Fork/属性测试
                              └─ WP8 Protocol ┴─ WP9 PlayerDriver ── WP10 FirstBoard
                                                        WP11 落盘 adapter（WP7 后任意时点）
```

WP0–WP3 为近期包，定义到可直接派发粒度；WP8 起为方向性描述，派发前由主线细化。**实现中发现设计文档与本文档冲突时，以实现中获得的新认知为准，但必须在完成报告中显式报告偏离。**

---

## 通用约定（适用于所有 WP）

- 遵守 D1：Kernel 及其测试不得引入 IO、wall-clock、环境读取；随机性只能来自显式注入的种子。
- 遵守 D6：net10.0、Nullable、ImplicitUsings、TreatWarningsAsErrors。
- 每个 WP 交付 = 代码 + 测试 + 完成报告（报告内容：做了什么 / 关键接口形状 / 偏离与疑问，写入完成报告而非新建文档）。
- 验收底线：`dotnet build` 与 `dotnet test` 在 repo 根目录全绿。
- 公共 API 用 XML doc 注释一句话说明语义；不写多余注释。
- 测试命名：`方法_场景_期望`（如 `Compare_SameTimeDifferentMicrostep_OrdersByMicrostep`）。

---

## WP0：Solution 骨架

**目标**：建立可构建、可测试、有 CI 的空 solution，为后续所有 WP 提供容器。

**范围内**：
1. repo 根目录 `DramaBoard.slnx`（优先 `dotnet new sln --format slnx`；如工具链报错回退 `.sln`）。
2. `Directory.Build.props`：`TargetFramework=net10.0`、`Nullable=enable`、`ImplicitUsings=enable`、`TreatWarningsAsErrors=true`、`LangVersion=latest`；`AssemblyName`/`RootNamespace` 统一为 `DramaBoard.$(MSBuildProjectName)` 形式——项目目录名即用 `Kernel`、`Kernel.Tests` 这样的短名。
3. `src/Kernel/`：空类库（可含一个占位类型，如 `internal static class KernelPlaceholder`，WP1 时删除）。
4. `tests/Kernel.Tests/`：xUnit 测试项目（优先 xUnit v3 模板 `dotnet new xunit3`；不可用则 v2），引用 Kernel，含一个恒真占位测试。
5. `.gitignore`（`dotnet new gitignore`）、`.editorconfig`（.NET 常规风格，4 空格缩进，UTF-8，LF）。
6. `.github/workflows/ci.yml`：push/PR 触发，matrix `{ubuntu-latest, windows-latest}`，步骤 checkout → setup-dotnet（10.0.x）→ `dotnet build -warnaserror` → `dotnet test`。
7. 根 `README.md`：三五行——项目一句话定位 + 指向 docs/ 阅读顺序（001→002→003→研发计划001→研发计划002）。

**范围外**：任何领域类型；NuGet 包引入（xUnit 除外）；git commit（由主线/用户执行）。

**验收标准**：
- `dotnet build` 与 `dotnet test` 根目录全绿；
- `dotnet build` 产出程序集名为 `DramaBoard.Kernel`、`DramaBoard.Kernel.Tests`；
- ci.yml 通过 YAML 语法检查（本地无法跑 Actions，结构正确即可）。

---

## WP1：时间语义 primitives

**目标**：实现 Design Note 003 §10–§11 的逻辑时间模型——这是整个 Kernel 最基础的地基。

**范围内**（目录 `src/Kernel/Time/`）：
1. `ModelTime`：世界逻辑时间。readonly struct，内部表示建议 `long` 计 tick（tick 粒度=1 秒或更细，需在 XML doc 中写死并说明理由），支持比较、加减 `ModelDuration`、`ToString` 人类可读（如 `D3 14:13:00`）。**不得**与 `DateTime`/`TimeSpan` 隐式互转。
2. `ModelDuration`：时长。readonly struct，同样基于 tick。
3. `Microstep`：同一 ModelTime 内的因果序，readonly struct 包 `int`。
4. `LogicalTimestamp`：`(ModelTime, Microstep)` 复合排序键，实现 `IComparable<LogicalTimestamp>`，全序。
5. 上述类型实现值相等语义（`IEquatable<T>`、`==`/`!=`、`GetHashCode`）。

**范围外**：wall-clock/presentation time（那是 Host/前端的事，Kernel 不知道它们存在）；日历系统（闰年月相之类，一律不做）。

**验收标准**：全序性质测试（自反、传递、与相等一致）；排序稳定性测试（同 ModelTime 不同 Microstep 按 Microstep 排）；加减运算测试含溢出边界行为（明确选择 checked 或饱和，测试锁定该选择）。

---

## WP2：确定性事件队列

**目标**：实现 Forecast 候选的存放与全局最早事件选择，语义严格确定：相同候选集合无论插入顺序如何，弹出顺序 bit 级一致。

**范围内**（目录 `src/Kernel/Scheduling/`）：
1. `EventCandidateId`：稳定标识（readonly struct 包 `long` 或 `Guid`——选 `long` 序列号，由队列所有者分配，保证确定性）。
2. `EventCandidate<TPayload>` 或非泛型 + object payload（倾向泛型，由实现权衡后在报告中说明）：含 `Id`、`Due`（LogicalTimestamp 或 ModelTime——见下方开放问题）、`SourceGeneration`、payload。
3. `ForecastQueue`：
   - `Enqueue` / `TryPeekEarliest` / `DequeueEarliest`；
   - **确定性 tie-break**：Due 相同时按稳定次序（如 `(Due, SourceSystemOrder, CandidateId)` 字典序），不依赖插入顺序或哈希序；
   - **staleness**：候选携带 `Generation`；提供按 source 失效（`InvalidateSource(sourceId, olderThanGeneration)`）或惰性失效（Dequeue 时校验 Generation 是否仍有效，无效则丢弃并记录）。第一版允许最简单实现：全量清空重建（003 §4 明确允许），但接口形状要为局部失效留位置。
4. 失效的候选可枚举（调试可观察性，003 §17）。

**范围外**：多线程安全（单线程运行是第一版明确假设）；性能优化。

**开放问题（实现时决策并报告）**：`EventCandidate.Due` 用 `ModelTime` 还是 `LogicalTimestamp`——候选只有时间、Microstep 是 Resolve 阶段才赋予的，还是候选就带 microstep？初步倾向：候选只带 `ModelTime`，Microstep 由 Kernel 在 Resolve 时按阶段规则分配（003 §11 的 μ0–μ4 是 resolve 阶段概念）。

**验收标准**：相同候选集不同插入顺序 → 相同弹出序列（穷举小集合的排列做测试）；Generation 失效语义测试；空队列/单元素/大量同时刻候选的边界测试。

---

## WP3：Kernel 主循环 MVP + Timer 玩具模型

**目标**：把 003 §3 的 Forecast → Advance → Resolve 循环跑起来，用《基础设施.md》Test Model A（Timer）验收。**本 WP 定义的接口是探索性的，预期在 WP4–WP6 中修订。**

**范围内**（目录 `src/Kernel/`）：
1. `ISimSystem`（名字可斟酌）：最小形状——
   ```csharp
   ForecastNext(worldView, now) → EventCandidate 集合
   Resolve(worldView, candidate) → DomainEvent 集合 + 状态变更描述
   ```
   具体签名由实现探索，但必须保持纯函数形状（D1）：不修改共享可变状态，变更以返回值表达。
2. `DomainEvent` 基础形状：`LogicalTimestamp` + 事件种类 + payload；进入 journal 后不可变。
3. `IJournalSink`（或 `IWorldJournal`）：`Append(DomainEvent)` + 可枚举已提交事件。第一实现 `InMemoryJournal`（就是个 List 包装）。
4. `SimulationLoop`：驱动循环——全量 Forecast → 选最早 → AdvanceTo → Resolve → Append → 重复，直到无候选或到达显式 `until` 时间。时间只能前进（`next.Due >= now` 违反即抛异常）。
5. Timer 玩具模型（放 `tests/Kernel.Tests/ToyModels/`）：三个独立 Timer 分别在 t=10、20、15 触发，断言 journal 顺序严格为 A(10)、C(15)、B(20)，且循环恰好推进三次时间跳跃（无逐 tick 空转）。

**范围外**：DecisionPoint / Player（WP9）；随机性（WP5）；Activity 中断（WP6）；superdense time 的完整 μ 阶段体系（本 WP 只需要"同一 ModelTime 的多个事件有确定 Microstep 序"这一最低保证）。

**验收标准**：Timer 模型测试通过；"时间不能倒退"不变量有专门测试（构造违规候选断言抛异常）；journal 中事件时间戳单调不减。

---

## WP4：Forecast 失效玩具模型（方向性，派发前细化）

《基础设施.md》Test Model B + C：
- **改道失效**：Alice 预定 t=10 到达 B，t=5 改道（作为外部注入的输入事件），原 arrival 候选必须失效，不得出现在 journal。
- **弹球模型**：N 个匀速圆球，Forecast 两两碰撞与撞墙时刻，推进到最早碰撞点、反弹、重新 Forecast。对错客观可验证（能量/动量守恒、与解析解对比）。这是对"预测-失效-重预测"循环的最强压力测试。

预期本 WP 会反馈修订 WP3 的 `ISimSystem` 接口——这是计划内的探索。

## WP5：Deterministic RNG + 随机 Forecast（方向性）

003 §12：`(StreamId, Generation)` 定位的稳定采样——相同 WorldState/Generation 下重复 Forecast 得到相同结果；Forecast 被多调用一次不得改变未来。可基于 splittable PRNG（如 SplitMix64 派生流）。玩具模型：挖矿随机发现事件，验证 replay 时随机事件时刻完全一致。

## WP6：Activity 与中断玩具模型（方向性）

《基础设施.md》Test Model D：Mining（+2h 完成）被 Alice 到达（+17min）打断，Activity 部分进度 materialize（003 §6 lazy materialization），重新 Forecast。探索 Activity 作为一等对象的表达。

## WP7：Replay/Fork harness + property tests（方向性）

- `Run(seed) → Journal A`；`Replay(Journal A) → 最终状态 B`；断言状态等价。
- `Fork(Journal A, event #k) + 不同后续输入 → 新时间线`。
- 引入 CsCheck，落实《基础设施.md》第六优先级的不变量清单（时间单调、相同输入相同 journal、stale forecast 不得成为 committed event 等）。

## WP8：DramaBoard.Protocol（方向性）

`src/Protocol/`：DecisionRequest、PlayerDecision、Intent、ExpectedOutcome、Observation 等 DTO（002 §6.1、《基础设施.md》第一优先级）。可序列化（System.Text.Json）、round-trip 无损、含 `BasedOnWorldVersion` 版本语义。零依赖（不引用 Kernel 也不被 Kernel 引用——Host 才是装配点；此点若实践中别扭，报告后调整）。

## WP9：IPlayerDriver + DecisionPoint 集成（方向性）

`IPlayerDriver.DecideAsync(DecisionRequest, CancellationToken)`；Scripted/Random/Null 三个实现；Kernel 循环产生 DecisionPoint 时经由 Host 询问 driver。注意：Kernel 本身保持同步纯函数，async 边界在 Host 层。

## WP10：FirstBoard 场景（方向性）

《基础设施.md》第七优先级：3 Places、2 Actors、1 Object、1 Secret、1 Deadline；动作 Travel/Wait/Talk/Observe/Take/Give；完整跑一局并输出 journal。

## WP11：EventJournal 落盘 adapter（方向性）

`src/Journal.Atelia/`：实现 `IJournalSink` 落盘到 `Atelia.EventJournal`（本地路径 `e:\repos\Atelia-org\atelia`）。引用机制（local NuGet vs ProjectReference）届时决策。这是全 solution 唯一允许引用 atelia 的项目（D3/D4）。
